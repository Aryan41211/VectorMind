# VectorMind — runtime + development dependencies
#
# Installing this file must be sufficient to run the test suite, the
# training scripts, and the serving stack. Exact pinned versions of a
# known-good environment live in requirements.lock.txt.
#
# GPU note: torch/torchvision here resolve to the default (CPU or
# system-CUDA) wheel. For an RTX-class GPU, install the build matching
# your CUDA version FIRST from https://pytorch.org/get-started/locally/,
# then run `pip install -r requirements.txt`.

# --- Core ML ---
torch>=2.2,<3.0
torchvision>=0.17,<1.0
numpy>=1.26,<3.0

# --- Data / preprocessing ---
# transformers is used ONLY for its tokenizer (see ARCHITECTURE.md §3);
# no pretrained model weights are loaded anywhere in the pipeline.
transformers>=4.40
tokenizers>=0.15,<1.0
Pillow>=10.0,<13.0
opencv-python>=4.9

# --- Config ---
pyyaml>=6.0,<7.0

# --- Experiment tracking (CLAUDE.md §5) ---
# TensorBoard is the project's tracker, chosen because it needs no
# account and writes to a local directory (ARCHITECTURE.md §11).
# wandb is deliberately NOT a dependency: nothing imports it.
tensorboard>=2.16

# --- Analysis / reporting ---
matplotlib>=3.8
# scikit-learn supplies t-SNE for the embedding-space figure
# (scripts/visualize_embedding_space.py). Nothing in the training or
# serving path imports it; it is a dependency of the reports and of
# tests/evaluation/test_projection.py, so CI needs it.
scikit-learn>=1.5

# --- Serving ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.6
faiss-cpu>=1.8
requests>=2.31,<3.0

# --- Testing / QA ---
pytest>=8.0,<9.0
httpx>=0.27
mypy>=1.10
ruff>=0.6

# Type stubs. mypy needs these to check third-party calls, and their
# absence is invisible locally once they are installed — CI failed on
# every push for a day because types-PyYAML was present here and not
# declared here.
types-PyYAML>=6.0
